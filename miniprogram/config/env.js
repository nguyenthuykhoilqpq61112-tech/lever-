module.exports = {
  // Set this to true after you create a WeChat CloudBase environment.
  enableCloud: false,

  // Replace with the real CloudBase environment id, for example: prod-xxxx.
  // Keep it empty while running the local-only consultation/prompt-copy flow.
  cloudEnvId: ''
}
